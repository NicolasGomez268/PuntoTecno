# Variables de entorno para Backend (Django)
# Copiá este archivo como .env en producción

# Seguridad
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True

# Hosts permitidos (separados por comas)
# Ejemplo: tu-usuario.pythonanywhere.com,www.tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS - Orígenes permitidos (separados por comas)
# Ejemplo: https://tu-app.vercel.app,https://www.tudominio.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Base de datos (SQLite por defecto)
# DATABASE_URL=sqlite:///db.sqlite3

# Para PostgreSQL en producción:
# DATABASE_URL=postgresql://usuario:password@host:puerto/nombre_bd
